import { createTheme } from '@material-ui/core/styles';
const theme = createTheme({
  shadows:['none'],
  palette: {
    // type:"dark",
    primary: {
      main: "#2F5D62",
    },
    secondary: {
      main: "#DFEEEA",
    },
  },
});
export default theme
